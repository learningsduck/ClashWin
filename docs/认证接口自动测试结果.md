# 认证接口自动测试结果

**测试时间**：自动执行  
**后端地址**：http://91.149.239.42:3001  

---

## 1. 健康检查 GET /health

| 项目     | 结果 |
|----------|------|
| 状态     | ✅ 通过 |
| 响应     | `{"ok": true}` |
| 说明     | 服务存活正常 |

---

## 2. 发送验证码 POST /auth/sms/send

| 项目     | 结果 |
|----------|------|
| 状态     | ✅ 接口正常 |
| 请求     | `{"phone":"13800138000"}` |
| 响应     | 返回 `TOO_FREQUENT`（请 42 秒后再试） |
| 说明     | 接口可用；因短时间内多次请求触发限频（60 秒内同一手机号只能发一次），符合预期 |

---

## 3. 登录 POST /auth/sms/login（错误验证码）

| 项目     | 结果 |
|----------|------|
| 状态     | ✅ 通过 |
| 请求     | `{"phone":"13800138000","code":"000000","device_id":"test-device"}` |
| 响应     | HTTP 400，`{"code":"CODE_INVALID","message":"验证码错误"}` |
| 说明     | 错误验证码被正确拒绝 |

---

## 结论

- **健康检查**：正常  
- **发送验证码**：正常（限频逻辑生效）  
- **登录校验**：正常（错误验证码返回 400 + CODE_INVALID）  

后端认证接口在 S2（91.149.239.42:3001）上运行正常，可用于前端联调与手动测试。
